import { CascadeDeleteInterceptor } from './cascade-delete.interceptor';

describe('CascadeDeleteInterceptor', () => {
  it('should be defined', () => {
    expect(new CascadeDeleteInterceptor()).toBeDefined();
  });
});
